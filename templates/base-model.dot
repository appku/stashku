/**
 * The class for `{{= it.metadata.name}}` instances within the "{{= it.metadata.namespace}}" namespace.
 *
 * **WARNING**    
 * THIS CLASS IS AUTOMATICALLY GENERATED DURING STASHKU EXTRACTS.    
 * CUSTOMIZATIONS WILL BE OVERWRITTEN ON NEW EXTRACTS.    
 * Use the non-autogenerated (base) class files to add unique customizations.
 */
class Base{{= it.metadata.name}} {
    constructor() {
        {{ for(var prop in it.properties) { }}
        {{=it.toJSDoc(it.properties[prop], 8)}}
        this.{{= prop}} = {{=it.defaultValue(it.properties[prop], true)}};
        {{ } }}
    }
    
}

export default Base{{= it.metadata.name}};
