///<reference path="./modeling.d.js" />
/**
 * The base class for `{{= it.name}}` instances for the "{{= it.resource}}" storage resource.
 * Exported: {{= it.timestamp.toISOString()}}
 *
 * **WARNING**    
 * THIS CLASS IS AUTOMATICALLY GENERATED DURING STASHKU OPTIONS EXPORTS.    
 * CUSTOMIZATIONS WILL BE OVERWRITTEN ON NEW OPTIONS EXPORTS.    
 * Use the non-autogenerated (base) class files to add unique customizations.
 */
class Base{{= it.name}} {
    constructor() {
        {{ for(let [prop, def] of it.mapping) { }}
        {{=it.makePropertyJSDoc(prop, def, 2)}}
        this.{{= prop}} = {{=it.toJavascriptPropertyDefaultReference(null, prop, def)}};
        {{ } }}
    }
    {{ for(let [prop, def] of it.mapping) { }}
    /**
     * StashKu property definition for {{=prop}}.
     * @type {Modeling.PropertyDefinition}
     */
    static get {{=prop}}() {
        return {{=it.toJavaScriptString(def, 2)}};
    }
    {{ } }}
    /**
     * The StashKu resource configuration for this model.
     * @type {Modeling.Configuration}
     */
    static get $stashku() {
        return {{=it.toJavaScriptString(it.config, 2)}};
    }
    
}

export default Base{{= it.name}};
