import Base{{= it.metadata.name}} from './base/base-{{= it.metadata.slug}}.js';

/**
 * The class for `{{= it.metadata.name}}` instances within the "{{= it.metadata.namespace}}" namespace.
 */
export default class {{= it.metadata.name}} extends Base{{= it.metadata.name}} {
    constructor() {
        super();
    }
    
    // This class is generated once if not found.
    // You can add customizations to this class and they will not be overwritten by subsequent extracts.

}