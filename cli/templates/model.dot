import Base{{= it.name}} from './base/base-{{= it.slug}}.js';

/**
 * The class for `{{= it.name}}` instances.
 */
class {{= it.name}} extends Base{{= it.name}} {
    constructor() {
        super();
    }
    
    // This class is generated once if not found.
    // You can add customizations to this class and they will not be overwritten by subsequent extracts.

}

export default {{= it.name}};